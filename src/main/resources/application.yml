spring:
  liquibase:
    enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: u
    password: 1

  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true